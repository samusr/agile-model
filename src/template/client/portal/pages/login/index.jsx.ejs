import "./index.scss";
import React, { Component } from "react";
import LoginForm from "./login-form";
import { Flash } from "../components";

export default class Login extends Component {
    render() {
        return (
            <div id="login" className="<%= portal %>-page">
                <Flash />
                <LoginForm />
            </div>
        );
    }
}
