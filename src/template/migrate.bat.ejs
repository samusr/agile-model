REM Windows batch file to set the DATABASE_URL environment variable and run the migration files

set DATABASE_URI=postgres://PG_USERNAME:PG_PASSWORD@127.0.0.1:5432/PG_DATABASE_NAME&&knex migrate:latest