const path = require("path");
const webpack = require("webpack");

module.exports = {
	context: process.cwd(),
	entry: {
		library: [ "enscript-reusables", "react", "react-dom", "react-redux", "react-router-dom", "redux", "redux-form", "redux-thunk"]
	},
	output: {
		filename: "agile-model-project.dll.js",
		path: path.resolve(__dirname, "./dist/lib"),
		library: "agileModelProject"
	},
	plugins: [
		new webpack.DllPlugin({
			name: "agileModelProject",
			path: path.resolve(__dirname, "./dist/lib/agile-model-project.json")
		})
	],
	resolve: {
		extensions: [".js", ".jsx", ".json", ".less", ".scss", ".css"],
		modules: [__dirname, "node_modules"]
	}
};
