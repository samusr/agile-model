const <%= model.name %> = require("../../../models/<%= model.filename %>");

module.exports = function(id, relationString = "") {
    return <%= model.name %>.query()
        .where({ id })
        .eager(relationString)
        .returning("*")
        .first();
};
