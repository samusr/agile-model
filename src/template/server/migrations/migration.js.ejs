//eslint-disable-next-line no-unused-vars
exports.up = function(knex, Promise) {
    return knex.schema.createTable("<%= model.tablename %>", table => {
		table.increments();<% for(const relation of model.relations) { if(relation.type == "BELONGS_TO_ONE") { %>
		table.integer("<%= relation.model.name.toLowerCase() %>_id").references("<%= relation.model.tablename %>.id").unique().index();<% }} %>
		table.timestamps(true, true);
    });
};

//eslint-disable-next-line no-unused-vars
exports.down = function(knex, Promise) {
	return knex.schema.dropTable("<%= model.tablename %>");
};
