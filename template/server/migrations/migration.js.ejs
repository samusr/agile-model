//eslint-disable-next-line no-unused-vars
exports.up = function(knex, Promise) {
	return knex.schema.createTable("<%= relation.sourceModel.tablename %>", table => {
		table.increments();<% for(const graph of relation.graphRelations) { for(const depdendencyModel of graph.dependencyModels) { if(graph.relationType.type == "BELONGS_TO_ONE") { %>
		table.integer("<%= depdendencyModel.modelName.toLowerCase() %>_id").references("<%= depdendencyModel.tablename %>.id").unique().index();<% }}} %>
		table.timestamps(true, true);
	});
};

//eslint-disable-next-line no-unused-vars
exports.down = function(knex, Promise) {
	return knex.schema.dropTable("<%= relation.sourceModel.tablename %>");
};